{
    "name": "realworld-rail",
    "license": "MIT",
    "engines": {
      "node": "10.16.0",
      "yarn": "1.16.0"
    },
    "scripts": {
      "build": "yarn --cwd client install && yarn --cwd client build",
      "deploy": "cp -a client/build/. public/",
      "heroku-postbuild": "echo $HEROKU_APP_NAME $HEROKU_PARENT_APP_NAME && yarn build && yarn deploy"
    }
}